import { Outlet } from "react-router-dom";
import Stopwatch from "../../components/stopWatch/StopWatch";

 
const Home = () => {
    return (
        <div>
            <Stopwatch/>
        </div>
    );
};

export default Home;